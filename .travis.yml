sudo: required
services:
    - docker

before_install:
    - docker build -t dhaniii/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true dhaniii/docker-react npm run test -- --coverage